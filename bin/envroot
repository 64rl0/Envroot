#!/bin/sh

if [ -z "${1}" ]; then
    echo "envroot: EXECUTABLE is not set" >&2
    exit 1
fi
if [ ! -x "${1}" ]; then
    echo "envroot: EXECUTABLE is not executable: ${1}" >&2
    exit 1
fi
exec "${1}" "$@"

